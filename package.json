{
  "name": "copy-this-note",
  "version": "0.8.1",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prebuild": "npm run lint && npm run typecheck",
    "build": "node esbuild.config.mjs production",
    "ci": "npm run check && npm test && npm run verify:release && npm run build:ci",
    "build:production": "node esbuild.config.mjs production",
    "build:ci": "node esbuild.config.mjs production --no-deploy",
    "prebuild:debug": "npm run lint && npm run typecheck",
    "build:debug": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify:release": "node scripts/verify-release.mjs",
    "release:check": "npm run verify:release && npm run build",
    "check": "npm run lint && npm run typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "@eslint/json": "0.14.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.30.1",
    "eslint-plugin-obsidianmd": "^0.3.0",
    "obsidian": "1.8.7",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.53.0",
    "vitest": "^4.1.6"
  }
}
